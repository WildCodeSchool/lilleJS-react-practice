import CatCard from "./CatCard";

const Home = () => {
  // ici tu dois créer un fetch a partir de cette url : "https://cataas.com/api/cats?tags=cute"
  // tu devras faire un map sur le resultat de ton tableau de data.
  // pour chaque chat tu dois avoir une CartMap (existe déja) - qui reçoit en propriétés le chat.
  // tips:  n'oublie pas la key (cat._id) par exemple.

  // si tout est fait correctement tu devrais voir des petits chats apparaitre 😸🎈

  return <div style={{ display: "flex" }}>LES PITITS CHATS ICI :</div>;
};

export default Home;
